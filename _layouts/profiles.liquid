---
layout: page
---
<style>
  .profile-left {
    display: flex;
    flex-direction: column;
    margin-right: 100px; /* 각 프로필 이미지의 우측 여백 설정 */
    margin-left: 50px; /* 각 프로필 이미지의 좌측 여백 설정 */
    margin-top: 30px; /* 제목과 아래쪽 요소 간의 간격 설정 */
    width: 280px;
  }

  .profile-right {
    margin-top: 80px; /* 제목과 아래쪽 요소 간의 간격 설정 */
  }

  .prof-name {
    font-size: 40px; /* 제목의 글자 크기 설정 */
    font-weight: bold; /* 제목을 두껍게 설정 */
    margin-top: 30px; /* 제목과 위쪽 요소 간의 간격 설정 */
    margin-bottom: 50px; /* 제목과 아래쪽 요소 간의 간격 설정 */
    color: #171036; /* 제목의 글자 색상 설정 */
  }

  .cv {
    display: flex;
    margin-top: 100px; /* 제목과 아래쪽 요소 간의 간격 설정 */
    margin-right: 10px; /* 각 프로필 이미지의 우측 여백 설정 */
  }
  
  .profile {
    width: 270px;
 /* 각 프로필 이미지의 우측 여백 설정 */
    margin-bottom: 20px; /* 프로필의 아래 간격 설정 */
  }

  .profile-name {
    font-size: 20px; /* 제목의 글자 크기 설정 */
    font-weight: bold; /* 제목을 두껍게 설정 */
    text-align: center;
    margin: 0; /* 기본 마진 제거 */
  }

  .profile img {
    width: 200px !important;
    height: 250px !important;
    margin-bottom: 20px !important; /* 각 프로필 이미지의 아래 여백 설정 */
    object-fit: cover !important;
  }

  .section-title {
    font-size: 40px; /* 제목의 글자 크기 설정 */
    font-weight: bold; /* 제목을 두껍게 설정 */
    margin-top: 30px; /* 제목과 위쪽 요소 간의 간격 설정 */
    margin-bottom: 30px; /* 제목과 아래쪽 요소 간의 간격 설정 */
    color: #171036; /* 제목의 글자 색상 설정 */
  }


  .clearfix::after {
    content: "";
    clear: both;
    display: table;
  }
  
  .flex-container {
    display: flex;
  }
</style>

<div class="post">
  <article>
    {% if page.profiles %}
      <div class="section">
        <h2 class="section-title">Professor</h2>
        {% for profile in page.profiles %}
          {% if profile.name == "Jongkil Kim" %}
            <div class="flex-container">
              <div class="profile-left">
                {% if profile.image %}
                  {% assign profile_image_path = profile.image | prepend: 'assets/img/' %}
                  {% if profile.image_circular %}
                    {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
                  {% else %}
                    {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
                  {% endif %}
                  {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.3 }}px, (min-width: 576px) 30vw, 95vw"{% endcapture %}
                  {% include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=profile.image %}
                {% endif %}
                {% if profile.more_info %}
                  <div class="more-info">{{ profile.more_info }}</div>
                {% endif %}
              </div>
              <div class="profile-right">
                {% if profile.name %}
                  <strong class="prof-name">{{ profile.name }}</strong>
                {% endif %}
                {% if profile.content %}
                  <div class="clearfix">
                    {% capture profile_content %}{% include_relative {{ profile.content }} %}{% endcapture %}
                    {{ profile_content | markdownify }}
                  </div>
                  <div class="cv">
                    {% for entry in site.data.cv %}
                      <a class="anchor" id="{{ entry.title }}"></a>
                      <div class="card mt-3 p-3">
                        <h3 class="card-title font-weight-medium">{{ entry.title }}</h3>
                        <div>
                          {% if entry.type == 'time_table' %}
                            {% include cv/time_table.liquid %}
                          {% else %}
                            {{ entry.contents }}
                          {% endif %}
                        </div>
                      </div>
                    {% endfor %}
                  </div>
                {% endif %}
              </div>
            </div>

           
            <div class="clearfix"></div>
            <hr>
          {% endif %}
        {% endfor %}
      </div>

      <!-- Repeat similar blocks for other sections without the specific layout for Jongkil Kim -->
      <div class="section">
        <h2 class="section-title">Ph.D student</h2>
        {% for profile in page.profiles %}
          {% if profile.name == "Djebbi, Ella" or profile.name == "Yeonhee Kim" %}
            <div class="profile float-left">
              {% if profile.image %}
                {% assign profile_image_path = profile.image | prepend: 'assets/img/' %}
                {% if profile.image_circular %}
                  {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
                {% else %}
                  {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
                {% endif %}
                {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.3 }}px, (min-width: 576px) 30vw, 95vw"{% endcapture %}
                {% include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=profile.image %}
              {% endif %}
              {% if profile.name %}
                <strong class="profile-name">{{ profile.name }}</strong>
              {% endif %}
              {% if profile.more_info %}
                <div class="more-info">{{ profile.more_info }}</div>
              {% endif %}
            </div>
          {% endif %}
        {% endfor %}
        <div class="clearfix"></div>
      </div>

      <div class="section">
        <h2 class="section-title">MS student</h2>
        {% for profile in page.profiles %}
          {% if profile.name == "Boeun Kim" or profile.name == "Oshunlola, Boluwatife Alimat"%}
            <div class="profile float-left">
              {% if profile.image %}
                {% assign profile_image_path = profile.image | prepend: 'assets/img/' %}
                {% if profile.image_circular %}
                  {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
                {% else %}
                  {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
                {% endif %}
                {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.3 }}px, (min-width: 576px) 30vw, 95vw"{% endcapture %}
                {% include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=profile.image %}
              {% endif %}
              {% if profile.name %}
                <strong class="profile-name">{{ profile.name }}</strong>
              {% endif %}
              {% if profile.more_info %}
                <div class="more-info">{{ profile.more_info }}</div>
              {% endif %}
            </div>
          {% endif %}

        {% endfor %}
        <div class="clearfix"></div>
      </div>

      <div class="section">
        <h2 class="section-title">Undergraduate student</h2>
        {% for profile in page.profiles %}
          {% if profile.name == "Jeeeun Park" or profile.name == "Hyunsol Mun" or profile.name == "Gyurim Kim"%}
            <div class="profile float-left">
              {% if profile.image %}
                {% assign profile_image_path = profile.image | prepend: 'assets/img/' %}
                {% if profile.image_circular %}
                  {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
                {% else %}
                  {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
                {% endif %}
                {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.3 }}px, (min-width: 576px) 30vw, 95vw"{% endcapture %}
                {% include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=profile.image %}
              {% endif %}
              {% if profile.name %}
                <strong class="profile-name">{{ profile.name }}</strong>
              {% endif %}
              {% if profile.more_info %}
                <div class="more-info">{{ profile.more_info }}</div>
              {% endif %}
            </div>
          {% endif %}
        {% endfor %}
        <div class="clearfix"></div>
      </div>
    {% endif %}
  </article>
</div>
